# PROMPTS — KeepKapook

ไฟล์เดียวจบ รวม `PROMPTS.md` เดิม + `PROMPTS_FIX_v2.md` + `PROMPTS_PHASE1.md`
**ลบสามไฟล์นั้นทิ้งได้** เรียงตามลำดับที่ต้องทำจริง ตัดรอบที่ทำไปแล้วออก

วิธีใช้: copy ทีละบล็อกไปวางใน Codex/Claude Code **ทีละรอบ** ห้ามรวบ
ทุก prompt สมมติว่า agent อ่าน `AGENTS.md` และ `PRODUCT_IDEA_BRIEF_v2.md` แล้ว

---

## สารบัญ

```
ก่อนเริ่ม   สัมภาษณ์ผู้ใช้ 8-10 คน + กรอก §2.1 ใน brief    ← agent ทำแทนไม่ได้

แก้ก่อนสร้าง (I-test ต้องเขียวก่อนไปต่อ)
  1   ยืนยันสถานะจริง                        ✅ เสร็จแล้ว
  2   validation ที่ domain (I5)             ✅ เสร็จแล้ว
  3   Cloud Pocket (I3)                    ✅ เสร็จแล้ว
  4   ดึงสูตรกราฟเป็น pure function (ปลด I7)  ✅ เสร็จแล้ว
  5   transaction model + schema v3           ✅ เสร็จแล้ว
  6   EXP / accounting (I6 + I7 + I13)      ✅ เสร็จแล้ว
  7   quest/badge + history snapshot        ✅ เสร็จแล้ว
  8   CI gate + I14                       ← รอบสุดท้ายก่อนประตู
  ═══ ประตู: I1-I14 เขียวครบ ═══

Phase 1 — พิสูจน์ว่าผู้ใช้กลับมา   (ลำดับมาจากผลสำรวจ brief §3.1)
  9   quick entry — บันทึกออม+รายจ่ายให้เร็วที่สุด
  10  streak + ปฏิทิน                    ← ตอบ "ลืม" คำตอบอันดับ 1
  11  local notification                 ← ตอบ "ลืม" คำตอบอันดับ 1
  12  weekly review + ตัวเชื่อมรายจ่าย→เป้าหมาย   ← หัวใจของจุดยืน
  13  CTA เป้าหมายถัดไปตอน celebration    ← ใหม่ เป้าจบใน 1-3 เดือน
  14  event tracking
  15  เตรียมปล่อยให้ผู้ใช้ทดสอบ
  ═══ ประตู Gate 1: วัดผล 4 สัปดาห์ ═══

Phase 2 — หลังผ่าน Gate 1 เท่านั้น
  16  reconciliation (ตรวจยอดจริง)
  17+ ตาม brief §11

ท้ายไฟล์  งานที่เลื่อนไว้ · ภาคผนวก: นิยาม I1-I12 · template · หลักการเขียน prompt
```

---

## สถานะที่ยืนยันแล้ว (อย่าทำซ้ำ)

จากผลเทส invariant รอบล่าสุด:

```
🟢 I1  money conservation ตอน transfer/allocate/withdraw
🟢 I2  external deposit เพิ่ม TOTAL ถูกต้อง
🟢 I4  goal ปกติล้นเข้า unallocated ถูกต้อง
🟢 I8  round trip toJson/fromJson ทุกโมเดล
🟢 I9  ข้อมูลพังถูกเก็บ backup + แจ้งผู้ใช้ ไม่ล้างเงียบ
🟢 I12 undo คืน TOTAL/EXP/quest/badge/transaction ครบ

🟢 I3  flexible pocket รับเงินได้แล้ว (แก้ในรอบ 3)
🟢 I5  validate ครบทุก action แล้ว (แก้ในรอบ 2)
🟢 I6  EXP ให้เฉพาะเงินเข้าใหม่แล้ว (แก้ในรอบ 6)
🟢 I7  กราฟนับเฉพาะ externalIn แล้ว (แก้ในรอบ 6)
🟢 I13 TxType ↔ flow ตรงกันเสมอ (เพิ่มในรอบ 6)

🟢 I10 ประวัติอ่านชื่อ goal ได้หลังลบ (แก้ในรอบ 7)
🟢 I11 ไม่มี quest/badge ที่ทำสำเร็จไม่ได้แล้ว (แก้ในรอบ 7)

✅ **13/13 เขียว · full suite 114/114** — เหลือรอบ 8 (CI + I14) ก่อนถึงประตู Phase 1
```

ทำไปแล้วจากงานรอบก่อน: onboarding + disclaimer · export/import JSON พร้อม validation ·
corrupt backup · undo infrastructure · เทส invariant I1-I12

**ยืนยันด้วยโค้ดจริงในรอบ 1 แล้ว (อย่าไปรื้อ):**

- เงินเป็น `int` หน่วย**สตางค์** ทุก field มี suffix `Satang`
  (`amountSatang`, `targetSatang`, `currentSatang`, `unallocatedSatang`)
- `schemaVersion = 2` · migration framework อยู่ที่ `lib/state/migrations.dart` รองรับต่อขั้น v1→v2
- storage key constant ชื่อ `appStateStorageKey`
- `double` ที่เหลือในโค้ดเป็น JSON helper และอัตราส่วน progress (0..1) **ไม่ใช่ยอดเงิน** ปล่อยไว้ได้

> รีวิวจากภายนอกที่บอกว่าเงินเป็น `double` หรือไม่มี `schemaVersion` อ่าน snapshot เก่า
> **ยึดผลรอบ 1 และผลเทส I1-I12 เป็นหลัก**

**ตัดสินใจแล้วในรอบ 2 (อย่ารื้อ):**

- **การรายงานความผิดพลาด** — `DomainValidationException` สำหรับ input ที่ผิดตั้งแต่ต้น
  (จำนวนเงินผิด, id ไม่มีอยู่, โอนเข้าตัวเอง) throw ก่อนแตะ state เสมอ ·
  **Result** สำหรับ business conflict ที่ผ่าน validation แล้วแต่ทำไม่ได้
  (เช่น แก้ประวัติไม่ได้เพราะยอดถูกใช้ไปแล้ว) — กฎอยู่ใน AGENTS.md
- **การเขียนข้อมูล** — `_save()` เป็น void, snapshot state ทันทีที่เรียก,
  debounce 300ms, เขียนผ่าน **ordered queue** จึงกัน state เก่าทับใหม่ได้ ·
  จุดที่ต้องรอจริงใช้ `await flushPendingSaves()` ·
  เขียนไม่สำเร็จไม่เงียบ (ข้อความไทย + MaterialBanner)
  ไม่มี retry อัตโนมัติ แต่ไม่จำเป็น เพราะทุก save เขียน state ทั้งก้อน
  การเขียนที่ล้มเหลวจึงหายเองเมื่อ mutation ถัดไปสำเร็จ

**ยืนยันในรอบ 3:**

- `flutter build apk --release` **ผ่านแล้ว** — toolchain Android พร้อม
  (ยังใช้ `com.example` อยู่ รอบ 15 เหลือแค่ config + icon ไม่ต้องลุ้นว่า build ได้ไหม)
- `flutter analyze lib` = 0 error, 0 warning, **16 info** ← ตัวเลขนี้คือสิ่งที่รอบ 8 ต้องจัดการ
  เมื่อเปิด `--fatal-infos`

**ยืนยันในรอบ 4:**

- สูตรการเงินทั้งหมดอยู่ที่ `lib/utils/financial_summary.dart` แล้ว (pure Dart, รับ `now` จาก caller)
  **ทุกรอบหลังจากนี้ห้ามเขียนสูตรใหม่ ให้เรียกตัวนี้** — รวมถึง weekly review และตัวเชื่อมในรอบ 12
- มี parity test กันตัวเลขเปลี่ยนตอน refactor แล้ว
- **I7 แดงยืนยันแล้ว**: โอนภายใน 2,000 สตางค์ → กราฟขยับ 500 → 2,500 นับเกินเท่ายอดโอนพอดี
- APK release = **53.3 MB** ← ใหญ่ไปสำหรับผู้ใช้มือถือไทย รอบ 15 ให้ใช้ `--split-per-abi`
  หรือ app bundle จะลดลงได้มาก

**ยืนยันในรอบ 5 — schema ปัจจุบันคือ v3:**

- มี `TransactionFlow` (externalIn / externalOut / internal / adjustment) แยกจาก `TxType` แล้ว
- `destinationGoalId` มีแล้ว · transfer ใหม่เก็บ id จริงและไม่เขียนข้อความลง `note`
- เลิก parse prefix `'โอนไป '` ทุกจุดแล้ว เหลือเฉพาะใน migration v2→v3 (ถูกต้อง)
- transfer เก่าที่ปลายทางเป็น null → ปิดการแก้ไข/ลบ พร้อมข้อความไทย

⚠️ **หนี้ที่รู้อยู่แล้วจาก legacy data (ไม่ใช่บั๊กใหม่):**

- v2 บันทึก allocate เป็น `TxType.deposit` → **allocate เก่ากับ deposit เก่าแยกกันไม่ออก**
  ทั้งคู่ถูก map เป็น `externalIn` แปลว่าหลังรอบ 6 กราฟจะถูกกับข้อมูลใหม่
  แต่ข้อมูลเก่าที่ migrate มาจะยังนับ allocate เป็นเงินเข้า
- v2 ไม่บันทึก `toUnallocated` ของ withdraw → withdraw เก่า map ตาม `TxType` อย่างเดียว
- กู้ปลายทาง transfer เก่าได้ 25% (1 จาก 4) ที่เหลือเป็น null โดยตั้งใจ
- **ไม่กระทบใครจริง** เพราะยังไม่มีผู้ใช้ production · ผู้ทดสอบรอบ 15 เริ่มจากศูนย์
  ถ้าตัวเลขเดือนเก่าในเครื่อง dev ดูผิด ให้ใช้ "ล้างข้อมูลทั้งหมด" ใน Settings

**ยืนยันในรอบ 6 — schema ปัจจุบันคือ v4:**

- ฝากเข้า unallocated ได้ EXP **ทันทีที่เงินเข้า** · allocate / transfer / deallocate ไม่แจก EXP ซ้ำ
- `q-allocate` ยังเก็บ progress ได้ EXP ตอน claim (ไม่ทับกับ EXP ตอน deposit)
- milestone 25/50/75/100% ยิงครั้งเดียว แม้เงินไหลออกแล้วกลับเข้า
- กราฟและค่าเฉลี่ยนับเฉพาะ `externalIn` ผ่าน `financial_summary.dart`
- **EXP เดิมของผู้ใช้ไม่ถูกหักแม้แต่แต้มเดียว** (หลัก "ห้ามลงโทษผู้ใช้")
- ผลข้างเคียงที่ตั้งใจ: กราฟ/ค่าเฉลี่ยของข้อมูลเก่าจะลดลง เพราะเลิกนับ transfer และ allocate ปน

**ยืนยันในรอบ 7 — schema ปัจจุบันคือ v5:**

- เพิ่ม `sourceGoalNameSnapshot` / `destinationGoalNameSnapshot` ในทุก transaction
  เลือกวิธีนี้แทน soft delete เพราะแตะแค่ 4 ไฟล์ แทนที่จะลามไปทุกหน้าจอที่ list goal
- ถอน `q-weekly-review`, `q-weekly-consistency`, `b-rhythm`, `b-memory` ออกแล้ว
  เงื่อนไขนำกลับมาอยู่ใน AGENTS.md §10 (รอบ 10 และ 12 ของไฟล์นี้จะเอากลับ)
- badge ที่ผู้ใช้ unlock ไปแล้วยังอยู่ · EXP และ quest progress ไม่ถูกหักหรือรีเซ็ต
- ข้อจำกัด: transaction ของ goal ที่ถูกลบ**ก่อน**อัปเกรด v5 กู้ชื่อไม่ได้ (ยอมรับแล้ว)

**ตัดสินใจแล้วเรื่อง positioning (ก่อนเริ่ม Phase 1):**

- ชู **สองฟีเจอร์ที่เป็นเรื่องเดียวกัน** — บันทึกรายรับรายจ่าย + การออมเพื่อเป้าหมาย
  ประโยคขาย: *"รู้ว่าเงินหายไปไหน แล้วเปลี่ยนมันเป็นเป้าหมายที่ถึงจริง"*
- รายรับรายจ่าย **ไม่ใช่ฟีเจอร์ที่สอง** แต่เป็นข้อมูลที่ทำให้การโค้ชการออมฉลาดขึ้น
  ห้ามสื่อสารว่าเป็น "แอปสองอย่าง" ให้สื่อสารว่าเป็นความเชื่อมโยงเสมอ
- **ตัวเชื่อมในรอบ 12 คือสิ่งเดียวที่ทำให้การจับคู่นี้มีเหตุผล** ถ้าไม่มี
  เราจะได้แอปธรรมดาสองตัวอยู่ในตัวเดียวกัน
- primary metric ยังเป็น **W4 logging retention ตัวเดียว** ไม่เพิ่มตัวที่สอง
  แต่รอบ 14 ต้องแยกนับว่าบันทึกออม vs รายจ่ายอย่างละกี่ครั้ง เผื่อ Gate 1 ออกมาแย่
  จะได้แยกได้ว่า loop ไหนไม่ทำงาน

**กระบวนการ — ทำทุกรอบ:**

- เอกสาร `AGENTS.md`, `PRODUCT_IDEA_BRIEF_v2.md`, `PROMPTS.md` **ต้องอยู่ใน repo**
  ไม่งั้น prompt ที่สั่งให้ "อ่าน brief §9 ก่อน" จะใช้ไม่ได้ (`PRODUCT_IDEA_BRIEF.md` v1 ลบทิ้งได้)
- แต่ละรอบแตก branch ของตัวเอง แล้ว **merge กลับ branch หลักทันทีที่ผ่าน DoD ของรอบนั้น**
  รอบถัดไปแตกจาก branch หลักที่อัปเดตแล้ว — ไม่งั้นรอบ 5→6 ที่ต่อยอดกันจะชนกัน

---

## ก่อนเริ่มรอบ 1 — งานที่ agent ทำแทนไม่ได้

1. **กรอก §2.1 ใน `PRODUCT_IDEA_BRIEF_v2.md`** — ทีม / เวลาต่อสัปดาห์ / นิยามความสำเร็จ
   ถ้ามีเวลา 5 ชม./สัปดาห์ ให้ตัด Phase 1 เหลือรอบ 9, 10, 11, 13 เท่านั้น

2. **สัมภาษณ์กลุ่มเป้าหมาย 8-10 คน คนละ 20 นาที**
   คำถามหลัก: *"ครั้งล่าสุดที่ตั้งใจเก็บเงินแล้วไม่สำเร็จ เกิดอะไรขึ้น"*
   ถ้าคำตอบส่วนใหญ่คือ "ไม่มีเงินเหลือให้เก็บ" ไม่ใช่ "ไม่รู้ว่าควรเก็บเท่าไร"
   → **หยุด** แล้วรื้อ brief ก่อน เพราะ coaching ไม่ใช่ทางแก้ของปัญหานั้น
   ใช้เวลารวมไม่ถึงหนึ่งวัน ถูกกว่าเขียน Phase 1 ผิดทางหลายสิบเท่า

---
---

# ส่วนที่ 1 — แก้ก่อนสร้าง

## รอบ 1 — ยืนยันสถานะจริง

```
งานสั้น ห้ามแก้โค้ด ยกเว้น AGENTS.md

รัน 3 คำสั่งแล้วแปะผลดิบมา:
  grep -rn "double " lib/models/ lib/state/
  grep -rn "schemaVersion" lib/
  grep -rn "_storageKey\s*=\|storageKey\s*=" lib/state/

ตอบ 3 คำถาม:
1. ยอดเงินเป็น int หรือ double — ถ้า int หน่วยคืออะไร (บาท หรือ สตางค์)
   ประกอบกับค่าในเทส I3 ที่คาด 251500 แต่ได้ 1500
2. มี schemaVersion ในข้อมูลที่ persist หรือยัง ตอนนี้เวอร์ชันอะไร
3. มี migration framework แยกไฟล์แล้วหรือยัง

จากนั้น:
- ถ้าเงินเป็น int แล้ว เขียนกฎลง AGENTS.md ให้ชัดว่าหน่วยคืออะไร
  และเปลี่ยนชื่อ field ให้บอกหน่วยได้ถ้าชื่อปัจจุบันกำกวม (ทำในรอบนี้ได้)
- ถ้ายังเป็น double >>> หยุดแล้วบอกฉัน <<< ต้องแทรกรอบแปลงเป็น int ก่อนรอบอื่น
- ถ้ายังไม่มี schemaVersion >>> หยุดแล้วบอกฉัน <<< ต้องแทรกรอบ migration framework
- แก้ทุกบรรทัดใน AGENTS.md และ brief §2 ที่ไม่ตรงกับความจริง (แก้ ไม่ใช่เติมหมายเหตุ)
```

## รอบ 2 — Validation ที่ domain boundary (I5)

> ด่วนที่สุด เพราะเทสบอกว่า **บาง action แก้ state ไปแล้วค่อยล้มเหลว**
> ไม่ใช่แค่ไม่ปฏิเสธ input — และ parser ที่กำลังจะมาคือแหล่ง input ที่เชื่อไม่ได้พอดี

```
รัน flutter test ดูรายละเอียด I5 ที่แดงทั้ง 6 ข้อ

สิ่งที่เทสยืนยันแล้ว:
- ทุก action ไม่ throw เลยสำหรับ zero / negative / เกินเพดาน
- addGoal เปลี่ยน state ด้วยค่าผิด
- transfer, allocate, withdraw เปลี่ยน state เมื่อค่าเกินเพดาน

งาน:
1. ตัดสินใจครั้งเดียวว่าจะใช้ exception หรือ Result type แล้วใช้แบบเดียวกันทั้งโปรเจกต์
   เขียนกฎลง AGENTS.md
   >>> ถ้าเห็นว่าควรเลือกอีกแบบ เสนอเหตุผลมาก่อนลงมือ <<<
2. validate ที่บรรทัดแรกของทุก method ที่รับจำนวนเงิน ก่อนแตะ state ใดๆ:
   addSaving, addGoal, addLedger, transfer, allocateUnallocated, withdraw, lock
   และตัวอื่นที่ grep เจอ
3. เงื่อนไขที่ต้องปฏิเสธ: <= 0 · NaN · infinity · เกินเพดานใน format.dart
   · goalId ที่ไม่มีอยู่ · โอนเข้าตัวเอง (from == to)
4. **หลักการที่สำคัญกว่าตัวเช็ก: ตรวจให้ครบก่อน แล้วค่อยเขียน state**
   ห้ามมี method ไหนแก้ state บางส่วนแล้วค่อยเจอว่าใช้ไม่ได้
   ถ้าโครงสร้างไม่เอื้อ ให้แยกเป็น validate() แล้วค่อย apply()
5. เลิกใช้ firstWhere ที่ throw StateError ได้ ทั่วโปรเจกต์ → คืน null แล้วจัดการเคส null
6. เก็บการเช็กใน UI ไว้ตามเดิม — UI กันไม่ให้ผู้ใช้เจอ error, domain กันความถูกต้อง

7. **ตรวจ _save() ไปพร้อมกันเลย** (คำถามนี้ตกไปจากรอบ 1)
   รายงานก่อนว่าตอนนี้ _save() ถูกเรียกแบบ await หรือไม่ · มี debounce ไหม · จับ error ไหม
   ถ้ายังไม่ครบ ให้แก้ในรอบนี้ เพราะกำลังเปิดทุก method ที่แก้ state อยู่แล้ว
   ต้นทุนเพิ่มแทบเป็นศูนย์ และรอบ 8 ที่เปิด unawaited_futures จะไม่ต้องกลับมาแก้ที่เดิมซ้ำ
   - mutation ต้องรอการเขียนเสร็จก่อน notifyListeners หรือใช้คิวที่รับประกันลำดับ
     (กัน race ตอนผู้ใช้กดเร็วๆ แล้ว state เก่าทับใหม่)
   - debounce ~300ms เพื่อไม่ให้เขียนถี่เกิน
   - เขียนไม่สำเร็จต้องไม่เงียบ

ข้อบังคับ:
- ห้ามเปลี่ยนพฤติกรรมของ input ที่ถูกต้องอยู่แล้ว
- ห้ามแตะ Cloud Pocket, EXP, หรือกราฟในรอบนี้
- I5 เขียวครบ 6 ข้อ และ I1, I2, I4, I8, I9, I12 ต้องยังเขียว

เกณฑ์เสร็จ: Definition of Done ใน AGENTS.md §6 + ตาราง I1-I12 ที่อัปเดตแล้ว
สรุปท้ายงาน: เลือก exception หรือ Result เพราะอะไร · method ไหนต้องแยก validate/apply ·
สถานะของ _save() ก่อนและหลังแก้
```

## รอบ 3 — Cloud Pocket (I3)

```
บั๊กที่ยืนยันแล้ว: ฝากเงินเข้า flexible pocket ที่มียอด 1500 อยู่
คาดว่าได้ 251500 แต่ยอดคงเป็น 1500 ส่วน 250000 ถูกส่งไป unallocated ทั้งก้อน

สาเหตุ: pocket สร้างด้วย targetAmount = 0 และ addSaving คำนวณ put = min(amount, remaining)
โดย remaining = clamp(target - current, 0, ∞) → pocket มี remaining = 0 เสมอ

ผู้ใช้เจอบั๊กนี้ทันทีที่ลองใช้ฟีเจอร์ที่ UI โฆษณาว่า "เข้า-ออกได้ตลอด"

งาน:
1. แยกพฤติกรรม flexible ออกจาก goal ที่มีเป้าหมาย: รับได้ไม่จำกัด ไม่มี overflow
2. ตรวจทุกจุดที่ใช้ remaining / progress / isCompleted ไม่ใช่แก้แค่ addSaving
3. progress ของ flexible ห้ามแสดงเป็น % (ไม่มีตัวหาร) ให้แสดงยอดสะสมแทน
4. milestone EXP และสถานะ "ถึงเป้า" ต้องไม่ทำงานกับ flexible
5. ตรวจว่า transfer เข้า/ออก flexible และ withdraw ทำงานถูก

ข้อบังคับ:
- ห้ามแก้ด้วยการตั้ง targetAmount เป็นค่ามหาศาล ต้องแยก branch ตาม flexible ให้ชัด
- I3 เขียว และ I4 ต้องยังเขียว
- ห้ามแตะ EXP หรือกราฟในรอบนี้

เกณฑ์เสร็จ: Definition of Done §6 + I3, I4 เขียว
```

## รอบ 4 — ดึงสูตรกราฟเป็น pure function (ปลด I7)

> รอบเล็กที่ให้ผลตอบแทนสูงสุดในไฟล์นี้

```
อ่าน PRODUCT_IDEA_BRIEF_v2.md §9 (Weekly review) ก่อน — เพื่อเข้าใจว่าทำไมต้องทำตอนนี้

ปัญหา: I7 ถูก skip เพราะสูตรคำนวณกราฟ 7 วันอยู่ในคลาส private _Chart
และอ่าน wall clock โดยตรง จึงเทสไม่ได้โดยไม่แก้ production

ไม่ใช่แค่เทสตกไป 1 ข้อ:
- บั๊กที่ว่ากราฟนับ transfer เป็นเงินออม **ยังไม่มีใครยืนยัน**
  (I1 ที่เขียวไม่ได้ตอบ เพราะ I1 วัด TOTAL ส่วนกราฟวัดยอดในช่วงเวลา คนละอย่างกัน)
- weekly review ในรอบ 12 จะคำนวณตัวเลขชุดเดียวกับกราฟนี้
  ปล่อยไว้แล้วเขียนด้วยแพตเทิร์นเดิม = ได้ของที่เทสไม่ได้เพิ่มอีกชิ้น

งาน:
1. ย้ายสูตรออกจาก _Chart ไปเป็น pure function ใน lib/utils/ ห้าม import Flutter
2. รับ now เป็นพารามิเตอร์ ห้ามเรียก DateTime.now() ข้างใน
3. _Chart เหลือหน้าที่วาดอย่างเดียว รับข้อมูลที่คำนวณเสร็จแล้วเข้ามา
4. ทำแบบเดียวกันกับทุกที่ที่คำนวณยอดในหน้าจอ: dashboard hero, สรุปเดือน, goal progress
   ทุกที่เรียก helper กลางตัวเดียวกัน ห้ามมีสูตรซ้ำสองที่
5. ปลด skip ของ I7 แล้วรัน
   >>> ถ้า I7 แดง แปลว่าบั๊กมีจริง — ห้ามแก้ในรอบนี้ <<<
   >>> ปล่อยแดงไว้แล้วรายงานมา บั๊กนั้นเป็นงานของรอบ 6 <<<

ข้อบังคับ:
- รอบนี้เป็น refactor ล้วน ห้ามเปลี่ยนตัวเลขที่ผู้ใช้เห็นแม้แต่บาทเดียว
  ถ้าตัวเลขเปลี่ยน แปลว่าย้ายผิด ให้ย้อนกลับมาดู
- ห้ามย้ายไฟล์หน้าจออื่น ห้ามเปลี่ยนโครงโฟลเดอร์ ห้ามเพิ่ม dependency

เกณฑ์เสร็จ: Definition of Done §6 + I7 รันได้ (เขียวหรือแดงก็ได้ แต่ต้องไม่ skip)
สรุปท้ายงาน: I7 ผลเป็นอย่างไร · ถ้าแดง กราฟนับอะไรเกินเข้ามา · helper อยู่ไฟล์ไหน
```

## รอบ 5 — Transaction model

> **รอบ 4 ยืนยันแล้วว่า I7 แดง** (กราฟนับ transfer เกิน 2,000 สตางค์)
> ขอบเขตรอบนี้ยังไม่ชัด ต้องให้ agent รายงานโครงสร้างก่อนตามข้อ 0

```
บริบท: I1 เขียวอยู่แล้ว แปลว่า transfer และ allocate รักษา TOTAL ถูกต้อง เงินไม่ได้ถูกสร้าง
ปัญหาคือระบบแยกไม่ออกว่ารายการไหนคือ "เงินเข้าจากภายนอก" กับ "ย้ายภายใน"
เวลาไปนับยอดและแจกรางวัล

งาน:

0. **รายงานก่อนลงมือ — หยุดรอถ้าคำตอบคือแบบหลัง**
   `SavingTransaction` ตอนนี้มี field อะไรบ้าง แปะ class declaration มาเลย
   - ถ้ามี field ปลายทางอยู่แล้ว (เช่น toGoalId หรือคล้ายกัน) → ทำข้อ 1 อย่างเดียว ข้อ 2 ข้าม
   - ถ้าปลายทางถูกเก็บไว้ใน `note` เป็นข้อความ → **หยุดแล้วบอกฉันก่อน**
     เพราะรอบนี้จะกลายเป็นงานรื้อ model + migration ที่ใหญ่กว่าที่วางไว้

1. เพิ่มมิติ "ทิศทางของเงิน" แยกจาก "ชนิดรายการ" ที่มีอยู่:
     externalIn   เงินเข้าระบบ (deposit, สลิป)
     externalOut  เงินออกจากระบบ (ถอนไปใช้จริง)
     internal     ย้ายภายใน ไม่เปลี่ยน TOTAL (allocate, transfer)
     adjustment   ปรับยอดให้ตรงความจริง (reconciliation รอบ 16 จะใช้)
   transaction เดิมทุกตัวต้องผ่าน migration
   ตัวที่เดาไม่ได้แน่ชัด ใช้กฎอนุรักษ์นิยมที่สุดแล้วบันทึกไว้ในสรุป

2. >>> ทำเฉพาะเมื่อรอบ 4 พบว่า transaction เก็บปลายทางไว้ใน note <<<
   เปลี่ยน goalId เดียวเป็น sourceGoalId + destinationGoalId (null ได้ทั้งคู่)
     deposit:  source=null,   destination=goalId, flow=externalIn
     allocate: source=null,   destination=goalId, flow=internal
     transfer: source=A,      destination=B,      flow=internal
     withdraw: source=goalId, destination=null,   flow=externalOut
   note เหลือไว้ให้ผู้ใช้พิมพ์เอง ห้ามเก็บข้อมูลโครงสร้างในนั้น
   ถ้ามี field รองรับอยู่แล้ว ให้ข้ามข้อนี้และรายงานมา

ข้อบังคับ:
- ต้องมี migration ทดสอบด้วยข้อมูลที่มี transfer อยู่แล้ว
- ห้ามแก้ EXP หรือการนับยอดในรอบนี้ (รอบ 6)
- ห้ามเพิ่ม field เผื่ออนาคตที่ไม่มีใครใช้ (adjustment ยกเว้นเพราะมีผู้ใช้แน่นอนในรอบ 16)
- I1, I2, I8 ต้องยังเขียว

เกณฑ์เสร็จ: Definition of Done §6
```

## รอบ 6 — EXP และการนับยอด (I6 + I7)

```
อ่าน PRODUCT_IDEA_BRIEF_v2.md §6 ข้อ 6 (ห้ามลงโทษผู้ใช้) ก่อน — สำคัญกับข้อบังคับท้ายรอบ

บั๊กที่เทสยืนยัน (I6):
- allocate เพิ่ม EXP 10
- ฝากเข้า unallocated ไม่ได้ EXP แล้วมาได้ตอน allocate ซึ่งผิดจังหวะ
- transfer ไม่เพิ่ม EXP อยู่แล้ว (ข้อนี้ถูก อย่าไปแก้)
บวกผล I7 จากรอบ 4 ถ้าแดง

งาน:
1. ใช้ flow จากรอบ 5 เป็นเกณฑ์เดียวทุกที่ ห้ามเช็ก type รายจุด
2. EXP ให้เฉพาะ externalIn — allocate และ transfer ต้องไม่ให้ EXP
3. **แก้จังหวะให้ถูก**: ฝากเข้า unallocated คือเงินเข้าระบบ ต้องได้ EXP ตอนนั้น
   ไม่ใช่ไปได้ตอนจัดสรร เพราะการจัดสรรคือการย้ายเงินตัวเอง
   (ถ้าเอา EXP ออกจาก allocate เฉยๆ โดยไม่ย้ายไปที่ deposit ผู้ใช้ที่ยังไม่จัดสรร
    จะไม่ได้ EXP เลย ซึ่งแย่กว่าเดิม)
4. quest ที่ให้รางวัลการจัดสรร (q-allocate) ยังให้ progress ได้ แต่ต้องไม่มาพร้อม EXP ซ้ำ
   แยกสองเรื่องนี้ให้ชัดในโค้ด
5. milestone EXP ของ goal ทำงานตามเดิม (25/50/75/100%)
   แต่ต้องไม่ยิงซ้ำเมื่อเงินไหลออกแล้วไหลกลับเข้ามา — เขียนเทสเคสนี้เพิ่ม
6. กราฟและยอดสรุปทุกตัวนับเฉพาะ externalIn (หัก externalOut ถ้าเหมาะ)
   แก้ที่ helper กลาง `lib/utils/financial_summary.dart` ที่เดียว ห้ามเขียนสูตรใหม่

7. **เพิ่ม invariant ใหม่ I13 — กัน TxType กับ flow ไม่ให้เพี้ยนจากกัน**
   ตอนนี้มีสองมิติที่เข้ารหัสข้อมูลทับกันบางส่วน ไม่มีอะไรกันไม่ให้ใครสร้าง transaction
   ที่ `type=transfer` แต่ `flow=externalIn` ซึ่งจะทำให้กราฟผิดแบบเงียบๆ อีกรอบ
   - เขียนตาราง mapping `TxType → flow` ไว้ที่เดียวเป็น source of truth
   - เทสยืนยันว่าทุก TxType map ได้ค่าเดียวเสมอ
   - เทสยืนยันว่า transaction ที่สร้างจาก **ทุก action ในระบบ** ผ่านกฎนี้
     (ให้ fail ถ้ามีคนเพิ่ม action ใหม่แล้วตั้ง flow เอง)
   - เพิ่มแถว I13 ลงตารางภาคผนวกท้ายไฟล์นี้

ข้อบังคับ:
- **ห้ามหัก EXP ย้อนหลัง** ถึงแม้การแก้จะทำให้ EXP เดิมของผู้ใช้ "ไม่ควรได้"
  ให้ค้างไว้แล้วเริ่มนับถูกจากนี้ไป (brief §6 ข้อ 6) บันทึกการตัดสินใจลง AGENTS.md
- ห้ามแก้ด้วยการเพิ่ม if กระจายในหน้าจอ ต้องแก้ที่แหล่งเดียว
- I6, I7 เขียว และ I1, I2, I12 ต้องยังเขียว

เกณฑ์เสร็จ: Definition of Done §6
สรุปท้ายงาน: ตัวเลขไหนในแอปจะเปลี่ยนไป และผู้ใช้เดิมจะเห็นอะไรต่าง
```

## รอบ 7 — ลบ quest และ badge ที่ทำสำเร็จไม่ได้ (I11)

```
อ่าน PRODUCT_IDEA_BRIEF_v2.md §6 ข้อ 6 ก่อน

**ข้อ 0 — ตัดสินใจขอบเขตก่อนลงมือ**
รอบนี้น่าจะต้องมี migration อยู่แล้ว เพราะผู้ใช้เดิมมี quest/badge ที่จะลบค้างอยู่ใน state
ถ้าใช่ **ให้ทำ I10 ในรอบเดียวกันเลย** (เพิ่ม snapshot ชื่อ goal ในทุก transaction
+ soft delete แทนการลบจริง) เพราะใช้ migration ร่วมกันได้ ประหยัด schema bump ไปหนึ่งรอบ
ถ้ารอบนี้ไม่ต้องมี migration จริงๆ ให้แยก I10 ออกไปทำต่างหากแล้วรายงานมา

เทสยืนยันว่าไม่มี handler สำหรับ:
  quest: q-allocate, q-weekly-review, q-weekly-consistency
  badge: b-rhythm, b-memory

ภารกิจที่ทำยังไงก็ไม่สำเร็จคือการลงโทษผู้ใช้แบบเงียบ
และผู้ใช้จะโทษตัวเองก่อนโทษแอป

งาน — ตัดสินใจทีละตัว ไม่มีทางที่สาม:
1. q-allocate           → มี event source แล้ว **ทำ handler ให้ครบ**
                           ระวังอย่าทับกับ EXP ที่แก้ในรอบ 6
2. q-weekly-review      → ยังไม่มี weekly review **ลบก่อน** กลับมาทำในรอบ 12
3. q-weekly-consistency → ต้องใช้ streak ที่ยังไม่มี **ลบก่อน** กลับมาทำในรอบ 10
4. b-rhythm             → ขึ้นกับ streak เช่นกัน **ลบก่อน** กลับมาทำในรอบ 10
5. b-memory (Album 10 รูป) → ไม่มี Album subsystem และไม่มีแผนใน Phase 1-2 **ลบ**

ทุกตัวที่ลบต้องจดลง Backlog ใน AGENTS.md §10 พร้อมเงื่อนไขว่าจะกลับมาเมื่อไร

6. ผู้ใช้เดิมที่มี quest/badge เหล่านี้ค้างอยู่:
   badge ที่เคย unlock แล้วห้ามหาย · quest ที่ลบต้องไม่ทำให้ข้อมูลผู้ใช้พัง
   เขียน migration ถ้าจำเป็น
7. ให้ I11 เป็นเทสถาวรที่ fail เมื่อมีคนเพิ่ม quest/badge ใหม่โดยลืมเขียน handler

ข้อบังคับ:
- ห้ามซ่อนด้วยการ comment ออก ให้ลบจริงและจดลง backlog
- ห้ามเหลือ quest หรือ badge ที่ทำสำเร็จไม่ได้ใน UI แม้แต่ตัวเดียว

เกณฑ์เสร็จ: Definition of Done §6 + I11 เขียว (และ I10 ด้วยถ้ารวมตามข้อ 0)
สรุปท้ายงาน: รวม I10 มาด้วยหรือไม่ เพราะอะไร · schema bump เป็นเวอร์ชันอะไร และเพิ่ม field อะไร
```

## รอบ 8 — CI gate

```
เป้าหมาย: ให้ CI เป็นคนตรวจ Definition of Done แทนคุณ ตั้งแต่ก่อนเริ่ม Phase 1
เพราะ Phase 1 จะเพิ่มโค้ดเยอะ และการตรวจมือทุกรอบจะหลุด

งาน:
1. GitHub Actions รันทุก PR:
     flutter analyze --fatal-infos --fatal-warnings
     flutter test
     flutter build web
   ทั้งสามต้องผ่านก่อน merge
2. ถ้าเปิด --fatal-infos แล้วมี diagnostic ค้างเยอะ (เช่น withOpacity, SwitchListTile.activeColor)
   >>> ห้ามปิด flag <<< เลือกอย่างใดอย่างหนึ่ง:
   (ก) แก้ทั้งหมดถ้าเป็นงาน mechanical ล้วน
   (ข) ignore เป็นรายไฟล์พร้อมเหตุผล แล้วรายงานจำนวนและรายชื่อมาเพื่อจดเป็น backlog
3. เปิด lint rule ที่ช่วยจับบั๊กจริง โดยเฉพาะ unawaited_futures
   (สำคัญกับ persistence race — ถ้าเจอ warning เยอะ แปลว่ามี race ที่ยังไม่ถูกแก้ ให้รายงาน)
4. อัปเดต AGENTS.md §6 ให้ตรงกับสิ่งที่ CI ตรวจจริง

5. **เพิ่ม I14 — migration ทั้งโซ่ต้องเดินได้**
   ตอนนี้ schema อยู่ที่ v5 มี migration 4 ขั้นต่อกัน (v1→v2→v3→v4→v5)
   เทสที่มีทดสอบแต่ละขั้นแยกกัน แต่ไม่มีอะไรพิสูจน์ว่าโซ่ทั้งเส้นทำงาน
   ขั้นหลังที่อ่าน field ซึ่งขั้นก่อนเพิ่งสร้าง คือจุดที่พังได้โดยเทสรายขั้นไม่จับ
   - สร้าง fixture ของข้อมูลจริงหนึ่งชุดต่อหนึ่ง schema version ที่เคยมี (v1, v2, v3, v4)
     เก็บเป็นไฟล์ JSON ใน test/fixtures/schema/
   - เทสยืนยันว่าทุก fixture migrate มาถึงเวอร์ชันปัจจุบันได้โดยไม่ throw
     และข้อมูลที่ควรอยู่ยังอยู่ (goal, transaction, EXP, badge ที่ unlock แล้ว)
   - เทสยืนยันว่า `TOTAL` หลัง migrate เท่ากับก่อน migrate ทุก fixture
   - **เมื่อ bump schema ครั้งหน้า ต้องเพิ่ม fixture ของเวอร์ชันก่อนหน้าด้วยเสมอ**
     เขียนกฎนี้ลง AGENTS.md §4
   - เพิ่มแถว I14 ลงตารางภาคผนวกท้ายไฟล์นี้

ข้อบังคับ:
- ห้ามแก้ logic ในรอบนี้ ถ้าการแก้ warning ต้องเปลี่ยน logic ให้จดไว้ทำแยก
- ห้าม commit secret หรือ token ลง workflow file

เกณฑ์เสร็จ: CI เขียวบน PR จริงอย่างน้อยหนึ่งครั้ง + แปะ log มา + I14 เขียว
```

---

## ═══ ประตูก่อน Phase 1 ═══

ต้องครบทุกข้อ:

- [ ] I1-I14 เขียวครบ ไม่มีข้อยกเว้น
- [ ] `flutter analyze --fatal-infos --fatal-warnings` ผ่าน หรือมี ignore ที่จดครบ
- [ ] CI เขียวบน PR จริง
- [ ] เปิดแอปด้วยข้อมูลเวอร์ชันเก่าแล้วยอดตรง
- [ ] ไม่มี quest/badge ที่ทำสำเร็จไม่ได้เหลือใน UI
- [ ] `AGENTS.md` ตรงกับโค้ดจริง

**กฎที่ต้องเพิ่มลง AGENTS.md ก่อนเริ่มรอบ 9:**

> logic ใหม่ทุกตัวใน Phase 1 ต้องเขียนเป็น pure function ใน `utils/` ที่เทสได้
> โดยไม่ต้องสร้าง AppState — AppState มีหน้าที่แค่เรียกและ persist
> **ห้ามมีสูตรคำนวณเพิ่มใน AppState หรือในคลาส widget อีกแม้แต่บรรทัดเดียว**

กฎนี้คือสิ่งที่ทำให้เลื่อนงาน refactor AppState ไปทีหลังได้โดยไม่กลายเป็นหนี้
AppState หยุดโตทันที และ weekly review ที่ต้องเป็น pure อยู่แล้วจะได้โครงที่ถูกตั้งแต่แรก

---
---
# ส่วนที่ 2 — Phase 1: พิสูจน์ว่าผู้ใช้กลับมา

> **ลำดับรอบนี้มาจากผลสำรวจจริง (brief §3.1)** — คำตอบอันดับหนึ่งของ
> "ทำไมเก็บเงินไม่สำเร็จ" คือ **"ลืม"** (4 จาก 8) และ **ไม่มีใครตอบว่า "ไม่รู้ว่าควรเก็บเท่าไร"**
> รอบ 10 (streak) และ 11 (notification) จึงมาก่อน parser
> ส่วน parser แก้แรงเสียดทานตอนบันทึก ซึ่งไม่มีใครระบุว่าเป็นสาเหตุที่ทำให้ล้มเหลว

## รอบ 9 — Quick entry: บันทึกให้เร็วที่สุด

```
อ่าน AGENTS.md และ PRODUCT_IDEA_BRIEF_v2.md §6 (หลักการ) และ §7 ให้จบก่อน

เป้าหมาย: ลดแรงเสียดทานตอนบันทึกให้ต่ำที่สุด ทั้งการออมและรายจ่าย

>>> parser เป็นความสะดวก ไม่ใช่จุดขาย <<<
จุดขายคือความเชื่อมโยงรายจ่าย→เป้าหมาย (รอบ 12) ไม่ใช่ความฉลาดของ parser
รอบนี้จึงเริ่มจากพื้นที่ปัญหาที่เล็กและทำให้ใช้ได้จริงก่อน อย่าทำเกิน

งาน:
1. ปุ่มจำนวนที่ใช้บ่อยสำหรับการออม (20 / 50 / 100) กดครั้งเดียวจบ
   เข้าถึงได้จาก FAB และจาก dashboard โดยไม่ต้องเปลี่ยนแท็บ
2. ช่องพิมพ์ประโยคเดียว รองรับทั้งการออมและรายจ่าย
3. parser แบบ deterministic ใน lib/utils/parser/
   - **pure Dart ห้าม import Flutter** เพื่อให้เทสเร็วและใช้ซ้ำได้
   - normalize: ช่องว่าง, เลขไทย ๐-๙, comma, ฿ / บ. / บาท, คำจำนวน พัน/หมื่น/แสน
   - **ต้องทนคำสะกดผิดตั้งแต่วันแรก** (ข้อมูลจริงจากแบบสอบถามมีคำผิดเยอะ)
   - classify ประเภท (ออม / รายจ่าย / รายรับ) จาก keyword dictionary ในไฟล์แยกที่แก้ง่าย
   - คืน confidence แยกรายฟิลด์ + overall tier: high / medium / low / reject
4. พฤติกรรมตาม tier (brief §6 — undo ชนะ confirm):
   high   → บันทึกทันที + snackbar + ปุ่ม "ยกเลิก" 5 วินาที
   medium → บันทึกทันที + chip แก้ได้ในบรรทัดเดียว เช่น [หมวด: อาหาร ▾]
   low    → ไม่บันทึก + ถามคำถามเดียวพร้อมปุ่มตัวเลือก
   reject → ไม่บันทึก + บอกว่าต้องการอะไรเพิ่ม
5. "ออม 300" ที่มีหลายกระปุก → ถามว่ากระปุกไหน (เป็นเคส low)
   ถ้ามีกระปุกเดียวบันทึกเลย
6. ทุกการบันทึกต้องเห็น progress หรือ EXP ขยับในหน้าจอเดียวกัน (brief §6 ข้อ 2)
7. แก้ไขและลบรายการย้อนหลังได้จาก history และ ledger ไม่ใช่แค่ undo ทันที

ชุดทดสอบ:
- test/fixtures/parser_corpus.dart
- **อย่างน้อย 20 ประโยคจริง** จากผู้ใช้จริง
  >>> ห้ามนับประโยคที่ AI หรือทีมแต่งเองเป็น corpus จริง (กฎใน AGENTS.md) <<<
  (ลดจากเกณฑ์เดิม 50 เพราะ parser ไม่ใช่จุดขายแล้ว พื้นที่ปัญหาเล็กลงมาก)
- เทสรันทั้ง corpus แล้วรายงาน accuracy แยกรายฟิลด์เป็นตัวเลข
- fail ถ้า accuracy ต่ำกว่า: จำนวนเงิน 98% / ประเภท 95% / หมวดหมู่ 80%
- **fail ทันทีถ้าเจอเคส tier=high ที่ผลลัพธ์ผิด — ต้องเป็น 0 เสมอ ไม่มีข้อยกเว้น**
  (คือกรณีที่แอปบันทึกเงินผิดโดยผู้ใช้ไม่รู้ตัว ทำลายความเชื่อถือถาวร)

ข้อบังคับ:
- ห้ามใช้ LLM หรือเรียก network — deterministic ล้วน
  (ส่งข้อความรายจ่ายออกไปหา LLM = ทำลาย positioning เรื่องข้อมูลอยู่ในเครื่อง)
- กำกวมเมื่อไหร่คืน tier=low พร้อมคำถาม ห้ามเดา
- logic ใหม่ทุกตัวเป็น pure function ใน utils/ ตามกฎในประตูก่อน Phase 1
- ห้ามเพิ่ม dependency ที่พัง flutter build web

เกณฑ์เสร็จ: Definition of Done §6 + I1-I14 ยังเขียว
+ widget test: ข้อความ high บันทึกแล้ว undo คืนค่าครบ (I12 ต้องยังเขียว)
+ widget test: ข้อความ low ไม่สร้างรายการ
สรุปท้ายงาน: ตาราง accuracy รายฟิลด์ · เคสที่ยังทำไม่ได้ · dictionary อยู่ไฟล์ไหน
```

> **corpus จะโตเองหลังปล่อย** — ทุกประโยคที่ได้ tier `low` / `reject`
> หรือถูกผู้ใช้แก้หลังบันทึก คือข้อมูลจริงที่ส่งตัวเองกลับมา (เก็บผ่าน event tracking รอบ 14)
> ให้ยกเกณฑ์ accuracy ขึ้นเมื่อ corpus โต

## รอบ 10 — Streak และปฏิทินการออม

> ตอบ "ลืม" ซึ่งเป็นคำตอบอันดับหนึ่งใน brief §3.1 โดยตรง

```
อ่าน AGENTS.md §4 (กฎเรื่องวันและเวลา) และ brief §6 ข้อ 6 (ห้ามลงโทษผู้ใช้)

เป้าหมาย: กลไก habit ตัวหลักที่ยังขาด ทำให้ผู้ใช้มีเหตุผลเปิดแอปวันนี้

นิยาม (ห้ามตีความเอง):
- "วันที่บันทึก" = วันที่มีรายการอย่างน้อย 1 รายการ (ledger หรือบันทึกเข้าเป้าหมาย)
- ขอบเขตวัน = local midnight ผ่าน helper กลางตัวเดียว
- streak = จำนวนวันติดต่อกันที่มีการบันทึก
- ผ่อนผัน 1 วัน: ขาด 1 วันไม่ตัด ขาด 2 วันติดกันตัด
  แสดงสถานะ "ผ่อนผันอยู่" ให้เห็นชัด ไม่ใช่ซ่อน
- **ห้ามลงโทษเมื่อ streak ขาด**: ไม่หัก EXP ไม่มีคำว่าล้มเหลว
  ข้อความต้องชวนกลับมา เช่น "เริ่มใหม่วันนี้ได้เลย"

กันการโกงเวลา: คำนวณจาก timestamp ของรายการที่บันทึกไว้เท่านั้น
ห้ามพึ่งค่า "วันสุดท้ายที่เปิดแอป" ที่ผู้ใช้หมุนนาฬิกาเครื่องแล้วเปลี่ยนได้

UI:
1. แสดง streak ปัจจุบันบน dashboard ใกล้ hero ไม่ใช่ซ่อนในหน้าอื่น
2. ปฏิทินรายเดือนที่ระบายวันที่มีการบันทึก แตะวันแล้วเห็นรายการของวันนั้น
3. แสดง streak ที่ยาวที่สุดที่เคยทำได้

เมื่อ streak มีแล้ว ให้เอา quest/badge ที่ถอนไปในรอบ 7 กลับมา:
q-weekly-consistency และ b-rhythm พร้อม handler ที่ครบ (I11 ต้องยังเขียว)

Persistence:
- คำนวณจากรายการที่มีอยู่ทุกครั้งถ้าทำได้ (ไม่ต้องเพิ่ม schema)
- ถ้าต้อง cache ให้ bump schemaVersion + migration + เพิ่ม fixture ตามกฎ I14
  และต้องคำนวณใหม่ได้เสมอถ้า cache หาย

เกณฑ์เสร็จ: Definition of Done §6 + I1-I14 ยังเขียว
+ unit test: ต่อเนื่อง / ขาด 1 วัน (ไม่ตัด) / ขาด 2 วัน (ตัด) / ข้ามเดือน / ข้ามปี
+ unit test: บันทึกย้อนหลังแล้ว streak คำนวณใหม่ถูกต้อง
```

## รอบ 11 — Local notification

> ตอบ "ลืม" โดยตรงเช่นกัน และเป็นรอบที่ราคาถูกที่สุดเทียบกับผลที่ได้

```
อ่าน AGENTS.md §7 (ห้ามทำ) — โดยเฉพาะข้อห้าม dependency ที่พัง web build

เป้าหมาย: เตือนให้กลับมาบันทึก โดยไม่ต้องมี backend และไม่ส่งข้อมูลออกนอกเครื่อง

งาน:
1. เพิ่ม flutter_local_notifications พร้อม conditional import + stub สำหรับ web
   **ยืนยันว่า flutter build web ยังผ่านก่อนทำอย่างอื่นต่อ**
2. เตือน 2 แบบ:
   - บันทึกประจำวัน (ผู้ใช้ตั้งเวลาเองได้ ค่าเริ่มต้น 20:00)
   - ดูสรุปสัปดาห์ (เช้าวันจันทร์ ค่าเริ่มต้น 09:00)
3. ขอ permission แบบมีบริบท: อย่าขอทันทีที่เปิดแอปครั้งแรก
   ให้ขอหลังผู้ใช้บันทึกรายการแรกสำเร็จ พร้อมอธิบายว่าจะเตือนเรื่องอะไร
4. ตั้งค่าใน Settings: เปิด/ปิดแต่ละแบบแยกกัน + เลือกเวลา
5. ข้อความต้องไม่กดดันและไม่ตำหนิ อ้างอิงสิ่งที่ผู้ใช้อยากได้
   เช่น "วันนี้บันทึกหรือยัง? อีก 23 วันถึง iPhone มือสอง"
   ห้ามใช้คำแนวเตือนหนี้หรือทวงถาม
6. ปฏิเสธ permission แล้วต้องไม่ถามซ้ำ และแอปใช้งานได้ปกติ

ข้อบังคับ:
- ห้ามส่งข้อมูลออกนอกเครื่อง ไม่มี push server
- บน web ปิดฟีเจอร์อย่างเงียบๆ ไม่ crash และไม่โชว์เมนูที่กดแล้วไม่ทำงาน
- ห้ามตั้งเตือนถี่กว่าวันละครั้งต่อประเภท

เกณฑ์เสร็จ: Definition of Done §6 + ยืนยันว่า flutter build web ผ่าน + I1-I14 ยังเขียว
สรุปท้ายงาน: ทดสอบบน platform ไหนได้จริงบ้าง และอะไรที่ยังทดสอบไม่ได้
```

## รอบ 12 — Weekly review + ตัวเชื่อมรายจ่าย→เป้าหมาย

> **รอบที่สำคัญที่สุดใน Phase 1** เพราะเป็นรอบที่ทำให้ "สองฟีเจอร์" กลายเป็น "หนึ่งเหตุผล"

```
อ่าน PRODUCT_IDEA_BRIEF_v2.md §4 (positioning) และ §9 ทั้งหัวข้อ + AGENTS.md §4

ทำไม weekly ไม่ใช่ monthly: monthly มี cold start — ผู้ใช้ที่ลงแอปวันที่ 3
จะได้รายงานแรกวันที่ 1 เดือนถัดไป คือเกือบ 30 วันหลังติดตั้ง
ซึ่งเลยจุดที่คนส่วนใหญ่เลิกใช้ไปแล้ว

งาน:
1. pure function รับรายการทั้งหมด + ช่วงเวลา คืน WeeklyReport
   อยู่ใน utils/ ห้ามคำนวณในหน้าจอ ห้ามเรียก DateTime.now() ข้างใน
   **ใช้ helper กลางใน lib/utils/financial_summary.dart ห้ามเขียนสูตรใหม่**
2. เนื้อหา: จำนวนวันที่บันทึก / streak / ยอดออมเข้าเป้า /
   รายจ่ายที่บันทึก + เทียบสัปดาห์ก่อน / หมวดสูงสุด /
   วันที่คาดว่าจะถึงเป้าตามจังหวะปัจจุบัน + เร็วหรือช้ากว่าแผนกี่วัน

3. >>> ตัวเชื่อม — ข้อนี้เป็นข้อบังคับ ถ้าไม่มี ถือว่ารอบนี้ไม่ผ่าน <<<

   สัปดาห์นี้ใช้ไป ฿2,180 (มากกว่าสัปดาห์ก่อน ฿240)
   ถ้าลดลงได้สัปดาห์ละ ฿200 "iPhone มือสอง" จะถึงเร็วขึ้น 11 วัน

   นี่คือสิ่งเดียวที่ทำให้การชูสองฟีเจอร์มีเหตุผล — ถ้าส่งรายรับรายจ่ายกับการออม
   ออกไปโดยไม่มีบรรทัดนี้ เราจะได้แอปธรรมดาสองตัวอยู่ในตัวเดียวกัน
   คำนวณจาก financial_summary.dart ที่คิดทั้งสองฝั่งได้อยู่แล้วตั้งแต่รอบ 4

4. First-week review: ผู้ใช้ใหม่ได้รายงานแรกวันที่ 7 หลังติดตั้ง ไม่ต้องรอวันจันทร์
   เนื้อหาเน้นคำถามเดียว "ถ้ารักษาจังหวะนี้จะถึงเป้าเมื่อไร"
5. Weekly ปกติ: แสดงเช้าวันจันทร์ ถ้าไม่เปิดแอปวันนั้นให้แสดงเมื่อเปิดครั้งแรกหลังจากนั้น
6. ดูรายงานย้อนหลังได้ ไม่ใช่เห็นครั้งเดียวแล้วหาย
7. ข้อมูลน้อยเกินจะสรุป → พูดตรงๆ ว่ายังสรุปไม่ได้ ห้ามเดาแนวโน้มจากจุดข้อมูลเดียว
8. เอา q-weekly-review ที่ถอนไปในรอบ 7 กลับมาพร้อม handler (I11 ต้องยังเขียว)

ข้อบังคับ:
- ทุกตัวเลขมาจาก pure function ที่มี unit test
- **ห้ามใช้คำว่า "เงินคงเหลือ"** ให้ใช้ "ยอดสุทธิจากรายการที่บันทึก"
- ต้องมีข้อความกำกับ "สรุปนี้คำนวณจากรายการที่คุณบันทึก ไม่ใช่ยอดเงินจริงจากธนาคาร"
- **ห้ามกล่าวเกินข้อมูลที่แอปรู้จริง** (brief §6 ข้อ 3) โดยเฉพาะตัวเชื่อมในข้อ 3
  ถ้าไม่มีข้อมูลรายจ่ายพอ ห้ามแสดงบรรทัดนั้น ดีกว่าแสดงตัวเลขที่เดามา
- คำนวณจาก LedgerEntry ทุกครั้ง ยังไม่ต้องเก็บ snapshot

เกณฑ์เสร็จ: Definition of Done §6 + I1-I14 ยังเขียว
+ unit test: สัปดาห์ที่ไม่มีข้อมูล / มีข้อมูลวันเดียว / ข้ามเดือน / ข้ามปี /
  เป้าหมายที่ถึงแล้ว / เป้าหมายที่ไม่มีกำหนดวัน
+ unit test ของตัวเชื่อม: ไม่มีรายจ่ายเลย / รายจ่ายลดลง / รายจ่ายเพิ่มขึ้น /
  ไม่มีเป้าหมายที่ยังไม่สำเร็จ
```

## รอบ 13 — CTA เป้าหมายถัดไปตอน celebration

> รอบใหม่ที่มาจากข้อมูลโดยตรง ไม่ได้อยู่ในแผนเดิม

```
อ่าน PRODUCT_IDEA_BRIEF_v2.md §3.1 และ §6 ข้อ 6 (ห้ามลงโทษผู้ใช้)

ที่มา: ผลสำรวจบอกว่า 5 จาก 9 ตั้งใจจะถึงเป้าหมาย "ภายใน 1-3 เดือน"
แปลว่าผู้ทดสอบจำนวนหนึ่งจะถึงเป้าระหว่างหน้าต่างวัด Gate 1 (4 สัปดาห์) พอดี
ถ้าไม่มีอะไรรับต่อ เขาจะหายไปหลังฉลอง แล้วเราจะอ่าน W4 retention ต่ำกว่าความจริง
ทั้งที่จริงๆ เขาสำเร็จ

งาน:
1. ตอน celebration ที่ถึงเป้าหมาย ให้เสนอเป้าหมายถัดไป **ในหน้าจอเดียวกัน**
   ไม่ใช่เด้งหน้าใหม่หลังปิด celebration
2. ถ้าผู้ใช้มีเป้าหมายอื่นที่ยังไม่สำเร็จค้างอยู่ → เสนออันนั้นก่อน
   ("เก็บต่อให้ <ชื่อเป้า> ไหม อีก ฿X ก็ถึงแล้ว")
   ถ้าไม่มี → เสนอให้สร้างใหม่ พร้อมทางลัดไปหน้าสร้างเป้า
3. ถ้ามียอด "ยังไม่จัดสรร" เหลืออยู่ ให้เสนอย้ายเข้าเป้าถัดไปได้เลยในคลิกเดียว
4. **ต้องมีปุ่ม "ไว้ก่อน" ที่ไม่มีผลเสียใดๆ** ไม่ตัด streak ไม่หัก EXP ไม่ขึ้นซ้ำรบกวน
   คนที่เพิ่งสำเร็จควรได้ฉลอง ไม่ใช่โดนขายของทันที
5. บันทึก event ว่ากี่ % รับข้อเสนอ / กี่ % กด "ไว้ก่อน" (ใช้ในรอบ 14)

ข้อบังคับ:
- น้ำเสียงต้องเป็นการชวน ไม่ใช่การกดดัน — ความสำเร็จต้องรู้สึกเหมือนความสำเร็จ
- ห้ามบล็อกไม่ให้ปิด celebration จนกว่าจะเลือก
- logic การเลือกว่าจะเสนออะไรต้องเป็น pure function ที่เทสได้

เกณฑ์เสร็จ: Definition of Done §6 + I1-I14 ยังเขียว
+ unit test: มีเป้าค้างอยู่ / ไม่มีเป้าอื่นเลย / มียอดยังไม่จัดสรร / ถึงเป้าพร้อมกันสองอัน
```

## รอบ 14 — Event tracking (ในเครื่องเท่านั้น)

```
อ่าน PRODUCT_IDEA_BRIEF_v2.md §12 (Metrics) และ §2

เป้าหมาย: วัดให้ได้ว่าผู้ใช้กลับมาไหม โดยไม่ทำลายสัญญาว่าข้อมูลอยู่ในเครื่อง

งาน:
1. เก็บ event แบบ "นับ" ไม่เก็บรายละเอียด:
   วันแรกที่ติดตั้ง / จำนวนวันที่บันทึก / สัปดาห์ที่ยังบันทึกอยู่ /
   จำนวนครั้งที่ใช้ quick entry แยกตาม tier /
   จำนวนครั้งที่กด undo / จำนวนครั้งที่แก้ไขรายการหลังบันทึก /
   จำนวนครั้งที่เปิด weekly review / จำนวนครั้งที่ทำตาม Recovery Plan /
   กี่ % รับข้อเสนอเป้าหมายถัดไป (จากรอบ 13)
2. **แยกนับว่าผู้ใช้บันทึก "การออม" กับ "รายจ่าย" อย่างละกี่ครั้ง**
   จำเป็นเพราะเราชูสองฟีเจอร์ ถ้า Gate 1 ออกมาแย่ ต้องแยกให้ได้ว่า loop ไหนไม่ทำงาน
3. คำนวณ W4 logging retention ได้จากข้อมูลนี้ (primary metric ตาม brief §12)
4. เก็บประโยคที่ parser ได้ tier low/reject และรายการที่ผู้ใช้แก้หลังบันทึก
   ไว้เป็น corpus จริงสำหรับปรับ parser รอบหน้า
   >>> เก็บเฉพาะข้อความที่ผู้ใช้พิมพ์ ไม่เก็บจำนวนเงินหรือชื่อรายการที่ระบุตัวตนได้ <<<
   และต้องมีสวิตช์ปิดใน Settings
5. รวมข้อมูลชุดนี้ไปกับ export JSON ที่มีอยู่แล้ว เพื่อให้ผู้ทดสอบส่งกลับมาได้ด้วยความสมัครใจ
6. หน้าใน Settings ให้ผู้ใช้ดูตัวเลขของตัวเองได้ทั้งหมด — โปร่งใส 100%

ข้อบังคับ:
- **ห้ามใส่ analytics SDK ของบุคคลที่สาม ห้ามส่งข้อมูลออกนอกเครื่องโดยอัตโนมัติ**
- ห้ามเก็บจำนวนเงินรายตัวลงใน event เก็บได้เฉพาะตัวนับและวันที่
- ต้อง bump schemaVersion + migration + เพิ่ม fixture ตามกฎ I14

เกณฑ์เสร็จ: Definition of Done §6 + I1-I14 ยังเขียว
สรุปท้ายงาน: W4 retention คำนวณจากฟิลด์ไหนบ้าง และดูได้ที่ไหนในแอป
```

## รอบ 15 — เตรียมปล่อยให้ผู้ใช้ทดสอบ

```
อ่าน AGENTS.md §7 (ห้ามทำ) ก่อน

เป้าหมาย: ให้คนอื่นติดตั้งและใช้ได้จริง เพื่อเก็บข้อมูล Gate 1
ทำทีละข้อและรายงานทุกไฟล์ที่แก้

1. **flush ตอนแอปเข้าพื้นหลัง** — งานค้างจากรอบ 2 ที่ต้องเสร็จก่อนปล่อย
   debounce 300ms + ผู้ใช้ปัดแอปทิ้งทันที = รายการล่าสุดอาจไม่ทันลง disk
   เรียก flushPendingSaves() เมื่อ AppLifecycleState เป็น paused / detached
   เป็น data loss จริงและเกิดง่ายกับ quick entry ในรอบ 9

2. เปลี่ยน applicationId / bundle id จาก com.example.keepkapook เป็น
       com.kukuythz.keepkapook
   (ตัดสินใจแล้ว ใช้ค่านี้ ห้ามเปลี่ยนเอง)
   แก้ครบทุกจุด: android/app/build.gradle (applicationId + namespace) ·
   path โฟลเดอร์ Kotlin ใต้ android/app/src/main/kotlin/ · AndroidManifest.xml ·
   iOS PRODUCT_BUNDLE_IDENTIFIER ทั้ง 3 target รวม RunnerTests
   >>> เช็กก่อนว่าว่าง: play.google.com/store/apps/details?id=com.kukuythz.keepkapook
       ถ้าไม่ 404 ให้หยุดแล้วบอกฉัน <<<

3. ลงทะเบียนแอปใหม่ใน Firebase project keepkapook-8fa2d2 เพื่อเอา
   google-services.json / GoogleService-Info.plist ตัวใหม่
   (ตัวเก่าผูกกับ com.example ใช้ไม่ได้)

4. app name บนเครื่อง + version/buildNumber ใน pubspec
5. app icon ทุกขนาด (Android + iOS + web favicon)
   ถ้ายังไม่มีไฟล์ต้นฉบับ ให้บอกว่าต้องการขนาดเท่าไร แล้วหยุดรอ
6. ตรวจ permission ใน AndroidManifest / Info.plist ให้เหลือเท่าที่ใช้จริง
   image_picker และ notification ต้องมีคำอธิบายเหตุผลภาษาไทยใน Info.plist
7. ลดขนาด APK — ปัจจุบัน 53.4 MB ใหญ่ไปสำหรับผู้ใช้ที่โหลดผ่านเน็ตมือถือ
   ใช้ --split-per-abi หรือ app bundle
8. **ตรวจนโยบายสโตร์เรื่องผู้ใช้อายุต่ำกว่า 18**
   ผลสำรวจ (brief §3.1) พบว่า 6 จาก 9 อายุต่ำกว่า 18
   Play Store / App Store มีข้อกำหนดเพิ่มเรื่อง data collection และ content rating
   สำหรับแอปที่มีผู้ใช้กลุ่มนี้ — ตรวจก่อนส่ง ไม่ใช่หลังโดนปฏิเสธ
9. เขียน RELEASE.md: checklist สิ่งที่ต้องทำด้วยมือ
   (signing key, Play Console, privacy policy URL, ช่องทางแจก APK ให้ผู้ทดสอบ)

ข้อบังคับ:
- ห้าม commit keystore, secret, หรือ config ที่มี key
- ห้ามเปลี่ยน bundle id เป็นค่าอื่นนอกจากที่ระบุไว้
```

---
---
## ═══ Gate 1 — วัดผล 4 สัปดาห์ ═══

หลังปล่อยให้ผู้ใช้ทดสอบครบ 4 สัปดาห์ ใช้ prompt นี้ประเมิน:

```
อ่าน PRODUCT_IDEA_BRIEF_v2.md §12 (Metrics และ Gate 1)

Phase 1 เสร็จแล้ว มีผู้ใช้ทดสอบ <N> คน เป็นเวลา <กี่สัปดาห์>
ข้อมูลจาก event tracking อยู่ที่ <แนบไฟล์ export หรือวางตัวเลขมา>

ช่วยวิเคราะห์:
1. ผ่าน Gate 1 หรือไม่ ดูรายเกณฑ์
2. ถ้าไม่ผ่าน สาเหตุน่าจะอยู่ตรงไหน — ตัว loop, parser, หรือ positioning
   และจะแยกแยะสามอย่างนี้ออกจากกันด้วยข้อมูลอะไร
3. ถ้าผ่าน อะไรใน Phase 2 ควรทำก่อน และอะไรควรตัดทิ้งถาวร
4. สมมติฐานข้อไหนใน brief §15 ที่ข้อมูลชุดนี้ตอบได้แล้ว และข้อไหนยังตอบไม่ได้

แยกคำตอบเป็น ข้อเท็จจริง / สมมติฐาน / ข้อเสนอ ให้ชัดเจน
ข้อมูลน้อยเกินจะสรุป ให้บอกตรงๆ ว่าต้องการอะไรเพิ่ม อย่าเดา
```

**เกณฑ์ Gate 1** (สมมติฐาน ปรับได้ตามบริบทจริง):

- ไปต่อ Phase 2 เมื่อ: W4 logging retention ≥ 25% · parser accuracy ของจำนวนเงิน ≥ 98%
  และ correction rate ≤ 20% · ≥ 40% ของผู้ใช้ที่ยังอยู่ เปิด weekly review อย่างน้อย 2 ครั้ง
- **ถ้า W4 < 15% → หยุด** อย่าเพิ่มฟีเจอร์ กลับไปคุยกับผู้ใช้ว่าทำไมไม่กลับมา
  ปัญหาไม่ได้อยู่ที่ฟีเจอร์ที่ยังไม่มี

---
---

# ส่วนที่ 3 — Phase 2 (หลังผ่าน Gate 1 เท่านั้น)

## รอบ 16 — Reconciliation (ตรวจยอดจริง)

> อย่าเริ่มจนกว่าจะผ่าน Gate 1

```
อ่าน PRODUCT_IDEA_BRIEF_v2.md §8 ทั้งหัวข้อ

เป้าหมาย: จัดการความเสี่ยงอันดับ 1 ของแอป — ยอดในแอปกับเงินจริงค่อยๆ ไม่ตรงกัน
โดยเปลี่ยนจุดอ่อนให้เป็นพิธีกรรมประจำเดือน แทนที่จะปล่อยให้ drift สะสมเงียบๆ

งาน:
1. ทุกสิ้นเดือน (หรือทุก 4 สัปดาห์) ชวนผู้ใช้ตรวจยอดจริงหนึ่งครั้ง
2. แสดงยอดที่แอปคิดว่าออมได้ + ช่องกรอกยอดจริง + ส่วนต่าง
3. ทางเลือก: [ปรับยอดในแอปให้ตรง] [เก็บไว้ก่อน] [ข้ามเดือนนี้]
4. ถ้าปรับ ให้สร้างรายการ adjustment (flow=adjustment จากรอบ 5) ที่ติดป้ายชัดเจน
   **ห้ามแก้หรือลบรายการเดิมย้อนหลัง**
5. เก็บประวัติการตรวจยอดไว้ดูย้อนหลังได้ (drift เปลี่ยนไปยังไงในแต่ละเดือน)

ข้อบังคับ (สำคัญกว่าตัวฟีเจอร์):
- สมัครใจเสมอ ข้ามได้ทุกเดือนโดยไม่มีผลใดๆ ห้ามบล็อกการใช้งาน
- ถามเฉพาะ "เงินที่คุณกันไว้ออม" **ห้ามถามยอดเงินทั้งหมดในบัญชี**
- ตัวเลขอยู่ในเครื่องเท่านั้น และต้องเขียนบอกผู้ใช้ตรงนั้นว่าไม่ได้เชื่อมบัญชี
- **ห้ามลงโทษ**: ไม่หัก EXP ไม่ตัด streak ไม่ใช้คำว่าผิดพลาดหรือล้มเหลว
- ส่วนต่างเป็นบวก (ออมได้มากกว่าที่จด) ให้ฉลอง ห้ามตำหนิว่าจดไม่ครบ
- ต้อง bump schemaVersion + migration

เกณฑ์เสร็จ: Definition of Done §6
+ unit test: ส่วนต่างบวก / ลบ / ศูนย์ / ข้ามหลายเดือน / ปรับยอดแล้วประวัติเดิมไม่เปลี่ยน
```

**รอบ 17+** — ตาม brief §11 Phase 2: monthly review + ดูย้อนหลัง ·
insight เชื่อมรายจ่ายกับวันถึงเป้า · shareable progress card · quick add · challenge รายเดือน

---
---

# งานที่เลื่อนไว้

แทรกระหว่างรอ feedback ผู้ใช้ หรือหลัง Gate 1:

- ~~flush ตอนแอปเข้าพื้นหลัง~~ → ย้ายไปเป็นข้อ 1 ของรอบ 15 แล้ว
- **category selector** ใน NewGoalScreen — `_cat` ไม่มี UI ให้เปลี่ยน ทุก goal เป็น other
  จำเป็นตอน Phase 2 ที่ทำ insight รายจ่ายแยกหมวด **ต่อ UI อย่าลบ field**
- **ลบ GoalPriority** ถ้ายืนยันว่าไม่มี consumer (อย่าเก็บ field เผื่ออนาคตใน persisted schema)
- **deprecated sweep** ที่เหลือหลังรอบ 8 — withOpacity → withValues(alpha:) ·
  SwitchListTile.activeColor · controller ใน dialog ที่ไม่ dispose
- **ดึง domain logic ออกจาก AppState** ส่วนที่รอบ 4 ยังไม่ได้แตะ
  (ทำเท่าที่จำเป็นเพื่อ testability **ห้ามย้ายไปโครง features/*/domain|data|presentation
   เต็มรูปแบบ** — โครงนั้นออกแบบมาสำหรับทีมหลายคน ต้นทุนสูงกว่าประโยชน์ตอนนี้)
- **ย้ายไป Drift/SQLite — ยังไม่ทำ**
  เหตุผล: ไม่แก้บั๊ก semantic สักข้อ · ปัญหา scale ยังไม่เกิดเพราะยังไม่มีผู้ใช้ ·
  และถ้ารื้อ transaction model แล้วค่อยย้าย จะต้อง migrate สองรอบ
  เกณฑ์ที่ควรเริ่มพิจารณา: รายการเกิน ~2,000 หรือวัดได้ว่า save ช้าเกิน 100ms
  จนผู้ใช้รู้สึก — **วัดก่อน ค่อยย้าย**
- **LINE integration** — เงื่อนไขปลดล็อกทั้ง 4 ข้อใน brief §10 ต้องเป็นจริงพร้อมกัน
- **OCR สลิปอัตโนมัติ** — google_mlkit_text_recognition เป็น mobile-only
  ต้อง conditional import + stub ไม่งั้น flutter build web พัง

---

# Template สำหรับเขียน prompt งานอื่นเอง

```
อ่าน AGENTS.md ในโปรเจกต์ก่อน โดยเฉพาะ §<หัวข้อที่เกี่ยว>

บริบท / ปัญหา: <ทำไมต้องทำ ไม่ใช่แค่ทำอะไร>

งาน:
1. <ขั้นตอนที่วัดผลได้>
2. ...

ข้อบังคับ:
- ทำเฉพาะขอบเขตนี้ เจอบั๊กนอกขอบเขตให้จดลง Backlog §10 ห้ามแก้พ่วง
- <ข้อห้ามเฉพาะงาน>

เกณฑ์เสร็จ: ตาม Definition of Done ใน AGENTS.md §6 + <รหัส I ที่ต้องเขียว>
สรุปท้ายงาน: ไฟล์ที่แตะ / การตัดสินใจที่ไม่ชัดในโจทย์ / สิ่งที่ยังไม่ได้ทำ
```

# หลักที่ทำให้ prompt ใช้ได้ผล

- **หนึ่งงานต่อหนึ่งรอบ** รวบหลายงาน agent จะทำครึ่งๆ กลางๆ ทุกงาน
- บอก **"ทำไม"** ไม่ใช่แค่ "ทำอะไร" — agent จะตัดสินใจถูกตอนเจอทางแยกที่ไม่ได้เขียนไว้
- ระบุ **ข้อห้าม** ชัดเจน ปัญหาที่เจอบ่อยที่สุดคือ agent แถม refactor มาจนรีวิวไม่ไหว
- ปิดท้ายด้วย **เกณฑ์เสร็จที่รันได้จริง** — ในโปรเจกต์นี้คือรหัส I1-I12 ไม่ใช่ "ให้เรียบร้อย"
- งานที่ตัดสินใจแทนไม่ได้ ให้เขียน `>>> ถามฉันก่อน <<<`
- **ตัวเลขเกณฑ์คุณภาพต้องอยู่ในเทส ไม่ใช่อยู่ในความหวัง** — accuracy 98% ต้องเป็น assertion
- รอบที่เป็น refactor ล้วน ให้สั่งชัดว่า **ห้ามแก้บั๊กที่เจอระหว่างทาง** ให้รายงานแทน
  ปนสองอย่างในรอบเดียวแล้วจะแยกไม่ออกว่าผลที่เปลี่ยนมาจากอะไร

---
---

# ภาคผนวก — นิยาม invariant I1–I12

รหัสเหล่านี้ถูกอ้างเป็น "เกณฑ์เสร็จ" ตลอดทั้งไฟล์ เทสจริงอยู่ใน `test/invariants/`
**เทสคือสเปกตัวจริง** ภาคผนวกนี้เป็นแค่คำอธิบายให้คนอ่านเข้าใจว่าแต่ละรหัสกันอะไรอยู่

นิยามกลาง: `TOTAL = sum(ทุก goal.currentAmount) + unallocated`

| รหัส | กันอะไร | เงื่อนไข |
|---|---|---|
| **I1** | เงินถูกสร้างจากการย้าย | หลัง transfer / allocate / ย้ายเงินภายในทุกชนิด `TOTAL` ต้องเท่าเดิมเป๊ะ |
| **I2** | เงินเข้าแล้วยอดไม่ตรง | deposit X → `TOTAL` เพิ่มขึ้น X พอดี ไม่มากไม่น้อย |
| **I3** | flexible pocket ไม่รับเงิน | ฝาก X เข้า pocket ที่ `flexible=true` → pocket เพิ่ม X, unallocated ไม่เปลี่ยน |
| **I4** | goal ปกติรับเกินเป้า | goal เหลือที่ R, ฝาก X > R → goal เพิ่ม R, unallocated เพิ่ม (X−R) |
| **I5** | ค่าที่ใช้ไม่ได้เข้าไปในระบบ | `<= 0` / NaN / infinity / เกินเพดาน → ปฏิเสธ **และ state ต้องไม่เปลี่ยนเลย** รวม EXP, quest, badge · ครอบทุก action |
| **I6** | ปั๊ม EXP ด้วยการย้ายเงินตัวเอง | EXP ให้เฉพาะเงินเข้าใหม่ · allocate และ transfer ต้องไม่เพิ่ม EXP · ฝาก 1000 แล้วจัดสรร 1000 → EXP เพิ่มครั้งเดียว |
| **I7** | กราฟนับเงินที่ไม่ได้เข้าใหม่ | transfer A→B → ยอดในกราฟ 7 วันต้องไม่เปลี่ยน |
| **I8** | ข้อมูลเพี้ยนตอน save/load | `toJson → fromJson → toJson` ได้ผลเท่ากันทุกโมเดล |
| **I9** | ข้อมูลผู้ใช้หายเงียบ | โหลด JSON ที่พัง → ต้องไม่ได้ state ว่างแบบเงียบๆ และต้องมี backup ของก้อนเดิม |
| **I10** | ประวัติกลายเป็น orphan | ลบ goal ที่มี transaction → history ยังแสดงชื่อได้ ไม่ขึ้นค่าว่างและไม่ throw |
| **I11** | ภารกิจที่ทำสำเร็จไม่ได้ | quest และ badge ทุกตัวใน default set ต้องมี code path ที่ทำ progress / unlock ได้ · **fail เมื่อมีคนเพิ่ม id ใหม่โดยลืมเขียน handler** |
| **I12** | undo คืนค่าไม่ครบ | undo → `TOTAL`, EXP, quest progress, badge, transaction กลับเป็นเหมือนก่อนบันทึก |
| **I13** | `TxType` กับ `TransactionFlow` เพี้ยนจากกัน | ทุก `TxType` map ไป flow ได้ค่าเดียวตามตาราง source of truth · transaction ที่สร้างจากทุก action ต้องผ่านกฎนี้ · **fail เมื่อมีคนเพิ่ม action ใหม่แล้วตั้ง flow เอง** |
| **I14** | migration ทั้งโซ่พัง ทั้งที่แต่ละขั้นผ่าน | fixture หนึ่งชุดต่อหนึ่ง schema version ที่เคยมี · ทุกชุดต้อง migrate ถึงเวอร์ชันปัจจุบันได้ ข้อมูลไม่หาย และ `TOTAL` เท่าเดิม · **bump schema ครั้งหน้าต้องเพิ่ม fixture ของเวอร์ชันก่อนหน้าเสมอ** |

## กฎของเทสชุดนี้

- แยกเป็นหลายไฟล์ใน `test/invariants/` ตามหมวด ห้ามยัดไฟล์เดียว
- ทุกเทสห้ามใช้ `DateTime.now()` ตรงๆ ให้ inject เวลาเข้าไป
- ถ้า API ปัจจุบันเทสไม่ได้เลย ให้เขียนเทสไว้แล้ว mark skip พร้อมคอมเมนต์ว่าติดอะไร
  **ห้ามข้ามไปเฉยๆ** — I7 ที่ skip อยู่คือตัวอย่างที่ทำถูก และมันเป็นตัวชี้เป้าให้รอบ 4
- **ห้ามลดเงื่อนไขเทสเพื่อให้ผ่าน** ถ้าเทสแดงเพราะโค้ดผิด ให้แก้โค้ด
  ถ้าคิดว่าเทสเองผิด ให้เสนอเหตุผลก่อน อย่าแก้เงียบๆ

## prompt สำหรับเพิ่ม invariant ใหม่

ใช้เมื่อเจอบั๊กประเภทที่เทสชุดนี้ยังไม่ครอบ — เขียนเทสก่อนแก้เสมอ

```
อ่าน AGENTS.md §6 และภาคผนวก I1-I12 ใน PROMPTS.md ก่อน

เจอพฤติกรรมที่น่าจะเป็นบั๊ก: <อธิบายสิ่งที่เห็น + วิธีทำให้เกิดซ้ำ>

งาน:
1. เขียนเทส invariant ตัวใหม่ในรูปแบบเดียวกับ I1-I12 ที่ test/invariants/
   ตั้งรหัสถัดจากตัวสุดท้ายที่มีอยู่ และเขียนให้ **fail** กับโค้ดปัจจุบัน
2. เพิ่มแถวในตารางภาคผนวกของ PROMPTS.md ว่ารหัสใหม่นี้กันอะไร
3. >>> ห้ามแก้โค้ด production ในรอบนี้ <<<
   เทสที่แดงคือสเปกของงานถัดไป ไม่ใช่ปัญหาที่ต้องรีบดับ
4. รายงานว่าเทสแดงเพราะอะไร และคาดว่าต้องแก้ตรงไหน

เกณฑ์เสร็จ: flutter test รันได้ · เทสใหม่แดงด้วยเหตุผลที่อธิบายได้ ·
เทสเดิมที่เคยเขียวยังเขียวทุกตัว
```
